@model IEnumerable<MtgApiManager.Lib.Model.Card>
@{
    ViewData["Title"] = "Home Page";
}

@if (Model != null)
{
    <ul>
    @foreach (var item in Model)
    {
        <li><img src="@item.ImageUrl" /></li>    
    }
    </ul>
}
else
{
    <p>Nothing here...</p>
}